(function ReadyJS(win, doc){
    'use strict';

    //let A = parseInt(prompt("Digite o primeiro numero inteiro"));
    //let B = parseInt(prompt("Digite o segundo numero inteiro!"));

    let multiplicar = function mult(A, B){
        return A * B;
    }

    //alert("O produto entre esses dois numeros eh: " + multiplicar(A,B));

})(window, document);