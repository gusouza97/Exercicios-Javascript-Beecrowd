(function ReadyJS(win, doc){
    'use strict';

    const pi = 3.14159;
    //let raio = parseFloat(prompt());

    let calcularArea = function calc(raio){
        return pi * (raio * raio);
    }

    //alert("A area do circulo é de " + calcularArea(raio));

})(window, document);