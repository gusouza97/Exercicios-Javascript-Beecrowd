(function ReadyJS(win, doc){
    'use strict';
    
    //let A = parseFloat(prompt());
    //let B = parseFloat(prompt());

    function sum(A, B){
        return A + B;
    }

    let result = sum(A,B);

    //alert(result);

})(window, document);